/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // ← use class-based dark mode
  content: [
    './src/**/*.{js,ts,jsx,tsx}', // adjust to your project paths
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
